idx=[6021]
len=6098
   ],
   "source": [
    "def verificar_pago_manual(df_export, key_lob_buscado, claims_buscado=None):\n    \"\"\"\n    Función auxiliar para verificar manualmente si un pago existe en la base final\n    \"\"\"\n    resultados = []\n\n    # Verificar por KEY LOB exacto\n    if 'KEY LOB' in df_export.columns:\n        mask_exacto = df_export['KEY LOB'].astype(str) == str(key_lob_buscado)\n        if mask_exacto.any():\n            resultados.append(f\"✓ Encontrado por KEY LOB exacto: {mask_exacto.sum()} registros\")\n\n    # Verificar por KEY LOB parcial (solo claims reference)\n    if key_lob_buscado and '-' in str(key_lob_buscado):\n        claims_part = str(key_lob_buscado).split('-')[0]\n        mask_parcial = df_export['KEY LOB'].astype(str).str.startswith(claims_part + '-')\n        if mask_parcial.any():\n            resultados.append(f\"✓ Encontrado por KEY LOB parcial: {mask_parcial.sum()} registros\")\n            ejemplo = df_export.loc[mask_parcial, 'KEY LOB'].iloc[0]\n            resultados.append(f\"  Ejemplo encontrado: '{ejemplo}'\")\n\n    # Verificar por claims reference directamente\n    if claims_buscado and 'CLAIM NUMBER' in df_export.columns:\n        mask_claims = df_export['CLAIM NUMBER'].astype(str) == str(claims_buscado).strip()\n        if mask_claims.any():\n            resultados.append(f\"✓ Encontrado por CLAIM NUMBER: {mask_claims.sum()} registros\")\n\n    if not resultados:\n        resultados.append(\"✗ No encontrado por ningún método\")\n\n    return resultados\n\ndef verificar_pago_manual(df_export, key_lob_buscado, claims_buscado=None):\n    \"\"\"\n    Función auxiliar para verificar manualmente si un pago existe en la base final\n    \"\"\"\n    resultados = []\n\n    # Verificar por KEY LOB exacto\n    if 'KEY LOB' in df_export.columns:\n        mask_exacto = df_export['KEY LOB'].astype(str) == str(key_lob_buscado)\n        if mask_exacto.any():\n            resultados.append(f\"✓ Encontrado por KEY LOB exacto: {mask_exacto.sum()} registros\")\n\n    # Verificar por KEY LOB parcial (solo claims reference)\n    if key_lob_buscado and '-' in str(key_lob_buscado):\n        claims_part = str(key_lob_buscado).split('-')[0]\n        mask_parcial = df_export['KEY LOB'].astype(str).str.startswith(claims_part + '-')\n        if mask_parcial.any():\n            resultados.append(f\"✓ Encontrado por KEY LOB parcial: {mask_parcial.sum()} registros\")\n            ejemplo = df_export.loc[mask_parcial, 'KEY LOB'].iloc[0]\n            resultados.append(f\"  Ejemplo encontrado: '{ejemplo}'\")\n\n    # Verificar por claims reference directamente\n    if claims_buscado and 'CLAIM NUMBER' in df_export.columns:\n        mask_claims = df_export['CLAIM NUMBER'].astype(str) == str(claims_buscado).strip()\n        if mask_claims.any():\n            resultados.append(f\"✓ Encontrado por CLAIM NUMBER: {mask_claims.sum()} registros\")\n\n    if not resultados:\n        resultados.append(\"✗ No encontrado por ningún método\")\n\n    return resultados\n\n\ndef validar_pagos_en_exportacion(df_export, payments_file_path, anomes, date_start_mes, date_start_suma):\n    \"\"\"\n    Valida que todos los pagos registrados en el archivo Payments_{anomes}.xlsx\n    estén presentes en el dataframe que se va a exportar.\n\n    Aplica los mismos filtros que el procesamiento real:\n    - Filtro por fecha (Payment request date >= date_start_suma)\n    - Filtro por LOB inward válidas para Marine\n    - Filtro por subsidiarias (excluir pólizas específicas)\n\n    Args:\n        df_export: DataFrame que se va a exportar\n        payments_file_path: Ruta del archivo de payments\n        anomes: Año y mes en formato YYYYMM\n        date_start_mes: Fecha de inicio del mes actual\n        date_start_suma: Fecha de inicio para suma (desde mes anterior)\n\n    Returns:\n        None si la validación es exitosa\n        Lanza excepción si hay pagos faltantes\n    \"\"\"\n\n    try:\n        # Verificar que el archivo de payments existe\n        if not os.path.exists(payments_file_path):\n            print(f\"⚠ ADVERTENCIA: No se encontró archivo de payments en: {payments_file_path}\")\n            print(f\"  Omitiendo validación de pagos\")\n            return\n\n        print(f\"\\n{'='*80}\")\n        print(f\"VALIDACIÓN DE PAGOS - MES {anomes}\")\n        print(f\"{'='*80}\")\n\n        # Leer el archivo de payments\n        excel_file = pd.ExcelFile(payments_file_path)\n        print(f\"✓ Archivo de payments encontrado: {payments_file_path}\")\n        print(f\"✓ Pestañas encontradas: {excel_file.sheet_names}\\n\")\n\n        # LOB inward válidas para Marine (igual que en el procesamiento)\n        lob_marine_validas = [\n            'CASCO Y MAQUINARIA', 'Casco y Maquinaria', 'CASCO',\n            'CARGO', 'PANDI', 'DW', 'Plataformas',\n            'DEEP WATERS', 'DEEPWATERS', 'Floteles'\n        ]\n\n        # Pólizas subsidiarias a excluir (igual que en el procesamiento)\n        subsidiarias_excluir = ['25300 30028443', '25300 30031974']\n\n        # Columnas clave para identificar pagos - múltiples variantes\n        columnas_policy = ['INWARD POLICY N°', 'Policy N°', 'POLICY N°', 'Policy Number']\n        columnas_claims = ['Claims Reference', 'CLAIMS REFERENCE', 'Claim Reference', 'claims_reference']\n        columnas_amount = ['Amount USD (CORRECT)', 'Amount USD', 'AMOUNT USD', 'Amount_USD']\n        columnas_lob = ['Lob-Inward', 'LOB-INWARD', 'LoB-Inward', 'LOB INWARD']\n        columnas_date = ['Payment request date', 'PAYMENT REQUEST DATE', 'Payment Request Date']\n\n        # Encontrar qué columnas existen en el dataframe (prioridad al nombre correcto)\n        col_policy = next((col for col in columnas_policy if col in df_export.columns), None)\n        col_claims = next((col for col in columnas_claims if col in df_export.columns), None)\n        col_amount = next((col for col in columnas_amount if col in df_export.columns), None)\n        col_currency = next((col for col in ['Currency', 'CURRENCY', 'Currency Code'] if col in df_export.columns), None)\n\n        if not any([col_policy, col_claims, col_amount]):\n            print(\"⚠ No se encontraron columnas de identificación de pagos en la exportación\")\n            print(f\"  Columnas disponibles en df_export: {list(df_export.columns[:30])}\")\n            return\n\n        print(f\"✓ Columnas detectadas para validación:\")\n        if col_policy:\n            print(f\"   - Policy: '{col_policy}'\")\n        if col_claims:\n            print(f\"   - Claims: '{col_claims}'\")\n        if col_amount:\n            print(f\"   - Amount: '{col_amount}'\")\n        print(f\"✓ Fecha de filtro: {date_start_suma.strftime('%Y-%m-%d')} en adelante (desde mes anterior)\")\n        print(f\"✓ LOB Marine válidas: {lob_marine_validas}\")\n        print(f\"✓ Pólizas subsidiarias excluidas: {subsidiarias_excluir}\")\n        print()\n\n        df_export_key_lob = None\n        if 'KEY LOB' in df_export.columns:\n            df_export_key_lob = df_export['KEY LOB'].astype(str).str.strip().str.upper()\n\n        pagos_faltantes = []\n        pagos_validados = 0\n        pagos_totales = 0\n        pagos_filtrados = 0\n\n        # Procesar cada pestaña del archivo de payments\n        for sheet_name in excel_file.sheet_names:\n            df_payments = pd.read_excel(payments_file_path, sheet_name=sheet_name)\n\n            if df_payments.empty:\n                print(f\"  Pestaña '{sheet_name}': vacía (0 pagos)\")\n                continue\n\n            pagos_filtrados += len(df_payments)\n            print(f\"  Pestaña '{sheet_name}': {len(df_payments)} pagos leídos\")\n\n            # Aplicar los MISMOS FILTROS que el procesamiento real\n            df_payments_filtrado = df_payments.copy()\n\n            # 1. FILTRO POR FECHA (igual que en el procesamiento)\n            col_fecha = next((col for col in columnas_date if col in df_payments_filtrado.columns), None)\n            if col_fecha:\n                # Convertir a datetime si no lo es\n                if not pd.api.types.is_datetime64_any_dtype(df_payments_filtrado[col_fecha]):\n                    df_payments_filtrado[col_fecha] = pd.to_datetime(df_payments_filtrado[col_fecha], errors='coerce')\n\n                # Filtrar por fecha (igual que en el procesamiento: date_start_suma)\n                df_payments_filtrado = df_payments_filtrado[\n                    (df_payments_filtrado[col_fecha] >= date_start_suma) |\n                    pd.isna(df_payments_filtrado[col_fecha])\n                ]\n                print(f\"    → Después de filtro fecha: {len(df_payments_filtrado)} pagos\")\n            else:\n                print(f\"  ⚠ Pestaña '{sheet_name}': No se encontró columna de fecha '{columnas_date}'\")\n\n            # 2. FILTRO POR LOB INWARD VÁLIDAS (igual que en el procesamiento)\n            col_lob = next((col for col in columnas_lob if col in df_payments_filtrado.columns), None)\n            if col_lob:\n                # Limpiar espacios y normalizar (igual que en el procesamiento)\n                df_payments_filtrado[col_lob] = df_payments_filtrado[col_lob].astype(str).str.strip().str.upper()\n\n                # Aplicar normalizaciones específicas (igual que en el procesamiento)\n                df_payments_filtrado.loc[df_payments_filtrado[col_lob] == 'P&I', col_lob] = 'PANDI'\n                df_payments_filtrado.loc[df_payments_filtrado[col_lob] == 'CASCO', col_lob] = 'CASCO Y MAQUINARIA'\n                df_payments_filtrado.loc[df_payments_filtrado[col_lob].str.contains('CASCO.*MAQUINARIA', na=False), col_lob] = 'CASCO Y MAQUINARIA'\n                df_payments_filtrado.loc[df_payments_filtrado[col_lob] == 'DW', col_lob] = 'DEEP WATERS'\n\n                # Filtrar por LOB válidas (igual que en el procesamiento)\n                df_payments_filtrado = df_payments_filtrado[df_payments_filtrado[col_lob].isin([lob.upper() for lob in lob_marine_validas])]\n                print(f\"    → Después de filtro LOB: {len(df_payments_filtrado)} pagos\")\n            else:\n                print(f\"  ⚠ Pestaña '{sheet_name}': No se encontró columna LOB '{columnas_lob}'\")\n\n            # 3. FILTRO POR SUBSIDIARIAS (igual que en el procesamiento)\n            col_policy_pagos = next((col for col in columnas_policy if col in df_payments_filtrado.columns), None)\n            if col_policy_pagos:\n                df_payments_filtrado = df_payments_filtrado[\n                    ~(df_payments_filtrado[col_policy_pagos].isin(subsidiarias_excluir))\n                ]\n                print(f\"    → Después de filtro subsidiarias: {len(df_payments_filtrado)} pagos\")\n\n            # Crear KEY LOB para comparación (igual que en el procesamiento)\n            col_claims_pagos = next((col for col in columnas_claims if col in df_payments_filtrado.columns), None)\n            if col_claims_pagos and col_lob:\n                df_payments_filtrado['KEY LOB VALIDACION'] = (\n                    df_payments_filtrado[col_claims_pagos].astype(str) + \"-\" +\n                    df_payments_filtrado[col_lob].astype(str)\n                )\n\n            print(f\"    → Pagos válidos para validación: {len(df_payments_filtrado)}\")\n\n            # Para cada pago filtrado en la hoja de payments\n            for idx, pago in df_payments_filtrado.iterrows():\n                pagos_totales += 1\n                encontrado = False\n                if 'KEY LOB VALIDACION' in pago.index and df_export_key_lob is not None:\n                    key_lob_pago = str(pago['KEY LOB VALIDACION']).strip().upper()\n\n                    # Verificar si este KEY LOB existe en la base final (df_export)\n                    if key_lob_pago in df_export_key_lob.values:\n                        encontrado = True\n                    else:\n                        # Verificar si hay algún registro con Claims Reference que coincida parcialmente\n                    else:\n                        # Verificar si hay algún registro con Claims Reference que coincida parcialmente\n                        claims_ref = str(pago.get(col_claims_pagos, '')).strip()\n                        if claims_ref and claims_ref != 'nan':\n                            # Buscar registros que contengan el claims reference\n                            mask_claims = df_export['CLAIM NUMBER'].astype(str).str.contains(claims_ref, na=False, case=False)\n                            if mask_claims.any():\n                                encontrado = True\n\n                # Si no se encontró por KEY LOB, intentar búsqueda alternativa más simple\n                if not encontrado and col_claims_pagos and col_claims:\n                    claims_val = str(pago.get(col_claims_pagos, '')).strip()\n                    if claims_val and claims_val != 'nan':\n                        # Buscar directamente por claims reference en la base final\n                        mask = df_export[col_claims].astype(str).str.strip() == claims_val\n                        if mask.any():\n                            encontrado = True\n\n                if encontrado:\n                    pagos_validados += 1\n                else:\n                    # Registrar el pago faltante con sus detalles\n                    detalles = {}\n                    if col_policy:\n                        detalles['Policy'] = pago.get(col_policy) if col_policy in pago.index else pago.get('Policy N°')\n                    if col_claims:\n                        detalles['Claims'] = pago.get(col_claims) if col_claims in pago.index else pago.get('Claims Reference')\n                    if col_amount:\n                        detalles['Amount'] = pago.get(col_amount) if col_amount in pago.index else (pago.get('Amount USD (CORRECT)') or pago.get('Amount USD'))\n                    if col_fecha and col_fecha in pago.index:\n                        detalles['Fecha'] = pago.get(col_fecha)\n                    if col_lob and col_lob in pago.index:\n                        detalles['LOB'] = pago.get(col_lob)\n                    if 'KEY LOB VALIDACION' in pago.index:\n                        detalles['KEY LOB'] = pago.get('KEY LOB VALIDACION')\n\n                    detalle_pago = {\n                        'hoja': sheet_name,\n                        'fila': idx + 2,  # +2 porque excel empieza en 1 y hay encabezado\n                        'detalles': detalles\n                    }\n                    pagos_faltantes.append(detalle_pago)\n\n        # Mostrar resultados\n        print(f\"\\n{'─'*80}\")\n        print(f\"RESULTADOS DE VALIDACIÓN:\")\n        print(f\"{'─'*80}\")\n        print(f\"  Total de pagos en archivo: {pagos_filtrados}\")\n        print(f\"  Pagos que pasaron filtros: {pagos_totales}\")\n        print(f\"  Pagos encontrados en exportación: {pagos_validados}\")\n        print(f\"  Pagos NO encontrados: {len(pagos_faltantes)}\")\n\n        if pagos_faltantes:\n            print(f\"\\n❌ ERROR: Hay {len(pagos_faltantes)} pagos que NO están en la exportación:\")\n            print(f\"   (Aplicando los mismos filtros que el procesamiento real)\\n\")\n\n            # Crear tabla con los pagos faltantes\n            pagos_faltantes_data = []\n            for pago_faltante in pagos_faltantes:\n                row = {\n                    'Pestaña': pago_faltante['hoja'],\n                    'Fila Excel': pago_faltante['fila']\n                }\n                # Agregar los detalles del pago\n                for col, valor in pago_faltante['detalles'].items():\n                    row[col] = valor\n                pagos_faltantes_data.append(row)\n\n            # Crear DataFrame y mostrar tabla\n            df_pagos_faltantes = pd.DataFrame(pagos_faltantes_data)\n\n            # Reordenar columnas para mejor presentación\n            cols_orden = ['Pestaña', 'Fila Excel', 'Policy', 'Claims', 'Amount', 'Fecha', 'LOB', 'KEY LOB']\n            cols_disponibles = [col for col in cols_orden if col in df_pagos_faltantes.columns]\n            cols_restantes = [col for col in df_pagos_faltantes.columns if col not in cols_disponibles]\n            cols_orden_final = cols_disponibles + cols_restantes\n\n            df_pagos_faltantes = df_pagos_faltantes[cols_orden_final]\n\n            print(\"📋 PAGOS FALTANTES EN LA EXPORTACIÓN:\")\n            print(\"=\" * 100)\n            print(df_pagos_faltantes.to_string(index=False))\n            print(\"=\" * 100)\n\n            # Agregar verificación manual para los primeros pagos faltantes\n            print(f\"\\n🔍 VERIFICACIÓN MANUAL DE PAGOS FALTANTES:\")\n            for i, pago in enumerate(pagos_faltantes[:5]):  # Solo verificar los primeros 5\n                key_lob = pago['detalles'].get('KEY LOB', 'N/A')\n                claims = pago['detalles'].get('Claims', 'N/A')\n\n                print(f\"\\n  Pago {i+1}: {pago['hoja']} - Fila {pago['fila']}\")\n                print(f\"    KEY LOB: '{key_lob}'\")\n                print(f\"    Claims: '{claims}'\")\n\n                # Usar la función auxiliar para verificar\n                resultados = verificar_pago_manual(df_export, key_lob, claims)\n                for resultado in resultados:\n                    print(f\"    {resultado}\")\n\n            # Mostrar resumen adicional\n            print(f\"\\n📊 RESUMEN DE PAGOS FALTANTES:\")\n            print(f\"   • Total de pagos faltantes: {len(pagos_faltantes)}\")\n            print(f\"   • Pestañas afectadas: {', '.join(sorted(set(df_pagos_faltantes['Pestaña'].dropna())))}\")\n\n            # Si hay montos, mostrar total faltante\n            if 'Amount' in df_pagos_faltantes.columns:\n                total_faltante = df_pagos_faltantes['Amount'].sum()\n                print(f\"   • Monto total faltante: USD {total_faltante:,.2f}\")\n\n            raise ValueError(\n                f\"\\n❌ VALIDACIÓN FALLIDA: Hay {len(pagos_faltantes)} pagos válidos \"\n                f\"que NO están en la exportación.\\n\"\n                f\"Estos pagos pasaron todos los filtros del procesamiento real pero no se encontraron en el resultado final.\\n\"\n                f\"Posibles causas:\\n\"\n                f\"  • El KEY LOB no coincide exactamente con el de la base principal\\n\"\n                f\"  • Los pagos no se incluyeron en el merge final\\n\"\n                f\"  • Error en el procesamiento de pagos\\n\"\n                f\"Por favor revisa el procesamiento de pagos antes de continuar.\"\n            )\n        else:\n            print(f\"\\n✓ VALIDACIÓN EXITOSA: Todos los {pagos_totales} pagos válidos\")\n            print(f\"  están presentes en la exportación\")\n            print(f\"{'='*80}\\n\")\n\n    except Exception as e:\n        if \"VALIDACIÓN FALLIDA\" in str(e):\n            raise\n        else:\n            print(f\"\\n⚠ Advertencia durante validación: {str(e)}\")\n            print(f\"  La validación se omitirá y continuará la ejecución\")\n\n\n# Ejecutar la validación antes de exportar\ntry:\n    # Usar la ruta correcta donde se guarda el archivo de payments\n    ruta_payments = os.path.join(route_account, f'Payments_{AñoMes}.xlsx')\n\n    validar_pagos_en_exportacion(df_update_db, ruta_payments, AñoMes, date_start_AñoMes, date_start_suma)\n\nexcept ValueError as e:\n    print(str(e))\n\n    print(\"\\n\" + \"=\"*80)    raise\n\n    print(\"EJECUCIÓN DETENIDA - Por favor corrige el procesamiento de pagos\")    print(\"=\"*80)"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},